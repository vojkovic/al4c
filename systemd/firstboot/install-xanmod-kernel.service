[Unit]
Description=Install Xanmod Kernel on First Boot
ConditionFirstBoot=yes

[Service]
Type=oneshot
ExecStart=yay -S --answerclean All --answerdiff None --noconfirm linux-xanmod-bin
ExecStart=grub-mkconfig -o /boot/grub/grub.cfg
ExecStart=shutdown -r now

[Install]
WantedBy=multi-user.target